# 프로젝트 기획서 — 피드백 분석 SaaS

---

## 1. 프로젝트 개요

**서비스명:** 피드백 분석  
**형태:** B2B SaaS (멀티테넌트 웹 애플리케이션)  
**한 줄 정의:** 고객사가 보유한 피드백 데이터를 업로드하면 자동으로 분류·분석하고 경영진 의사결정용 리포트를 생성해 주는 서비스

---

## 2. 문제 정의

중소·중견 기업은 리뷰, 문의, 불만 데이터를 CRM / 앱스토어 / 자체 게시판 등 **다양한 소스에서 수집**하지만,

- 데이터 형식이 제각각(CSV, Excel, TXT, 컬럼명 불일치)이라 통합이 어렵고
- 수작업 분류에 시간이 많이 걸리며
- 어떤 카테고리의 불만이 급증하고 있는지 **빠르게 파악할 수단이 없음**

결과적으로 고객의 소리(VOC)가 제품 개선에 반영되는 데까지 시간이 과도하게 소요됩니다.

---

## 3. 솔루션

파일을 업로드하면 **3단계 위저드**가 컬럼 구조 차이를 흡수하고, 자동 분석 엔진이 각 피드백을 분류·감정 판별합니다.  
결과는 대시보드와 기간별 리포트로 즉시 확인할 수 있습니다.

```
데이터 업로드 → 컬럼 매핑 → 자동 분류·분석 → 대시보드 / 리포트
```

---

## 4. 주요 기능

### 4-1. 유연한 파일 업로드
- 지원 형식: CSV, XLSX, XLS, TXT (최대 100MB, ~35만 행)
- **3단계 컬럼 매핑 위저드**: 어떤 컬럼명이든 시스템 필드에 연결 가능
- 컬럼명 자동 감지 (흔한 패턴 80여 개 매칭)
- 500행 단위 청크 스트리밍 처리 — 메모리 효율적

### 4-2. 스마트 분석 엔진
- **카테고리 분류**: 배송·환불·품질·결제·고객센터 등 8개 카테고리, 키워드 가중치 합산
- **감정 분류**: 부정/중립/긍정, 평점 보정 신호 포함
- **키워드 추출**: 복합 패턴(배송 지연, 이중 결제 등) 우선 추출
- **우선순위 산정**: 감정 + 카테고리 긴급도 + 평점 + 키워드 수 합산 → high/medium/low
- LLM 교체 가능 설계 (`analyze()` 함수 본문만 교체)

### 4-3. 대시보드
- 전체 피드백 수, 부정 비율, 감정 분포 도넛 차트
- 카테고리별 피드백 가로 막대 (TOP 5 기본, 전체 토글)
- 7일/30일 일별 추이 라인 차트
- 제품·지점별 부정 피드백 순위

### 4-4. 피드백 목록
- 날짜·감정·카테고리·유형·키워드 복합 필터
- 페이지네이션 (페이지당 20건)

### 4-5. 리포트
- 기간 지정 → TOP 이슈, 급증 키워드, 제품별 부정 순위, 개선 제안 자동 생성
- 리포트 목록 관리 (생성·삭제)

---

## 5. 기술 스택 및 선택 이유

### Frontend

| 기술 | 선택 이유 |
|---|---|
| **Next.js 15 (App Router)** | 파일 기반 라우팅, 레이아웃 공유, SSR/CSR 유연 선택. B2B 대시보드에 최적 |
| **TypeScript** | API 응답 타입 명시로 런타임 오류 사전 차단 |
| **Tailwind CSS** | 유틸리티 클래스로 빠른 UI 구성, 커스텀 디자인 시스템 불필요 |
| **Recharts** | React 친화적 차트 라이브러리. 감정 분포 도넛, 카테고리 바, 추이 라인 등 |
| **Axios** | 인터셉터로 JWT 자동 삽입·401 리다이렉트 일괄 처리 |

### Backend

| 기술 | 선택 이유 |
|---|---|
| **FastAPI** | 타입 힌트 기반 자동 문서화(Swagger), 비동기 지원, Python 생태계 활용 |
| **SQLAlchemy + SQLite** | MVP 단계에서 설정 없이 즉시 사용 가능. ORM 추상화로 추후 PostgreSQL 이전 용이 |
| **pandas** | CSV/Excel/TXT 다형식 파싱, chunksize 스트리밍, 컬럼 자동 감지 |
| **python-jose** | JWT 발급·검증. HS256 서명 |
| **bcrypt** | 패스워드 해싱. passlib 호환성 이슈 회피를 위해 직접 임포트 |
| **uv** | pip 대비 10~100× 빠른 의존성 설치·가상환경 관리 |

---

## 6. 시스템 아키텍처

```
┌─────────────────────────────────────────────────────┐
│                   Browser (Client)                  │
│  Next.js 15 App Router  ·  Tailwind  ·  Recharts    │
│                                                     │
│  /login  /dashboard  /upload  /voc  /reports        │
└──────────────────────┬──────────────────────────────┘
                       │ HTTP (JSON / multipart)
                       │ Authorization: Bearer <JWT>
┌──────────────────────▼──────────────────────────────┐
│                  FastAPI Server                     │
│  :8000                                              │
│                                                     │
│  POST /auth/login      GET  /auth/me                │
│  POST /voc/preview     POST /voc/upload             │
│  GET  /voc/records     GET  /voc/dashboard          │
│  POST /reports/generate  DELETE /reports/{id}       │
└──────────┬─────────────────────┬────────────────────┘
           │                     │
┌──────────▼──────────┐ ┌────────▼────────────────────┐
│   services/         │ │   SQLite (voc.db)            │
│   analysis.py       │ │                             │
│   ─ 카테고리 분류   │ │   users                     │
│   ─ 감정 분류       │ │   voc_records               │
│   ─ 키워드 추출     │ │   voc_analyses              │
│   ─ 우선순위 산정   │ │   reports                   │
│                     │ │                             │
│   report.py         │ └─────────────────────────────┘
│   ─ 리포트 생성     │
└─────────────────────┘
```

### 데이터 흐름 (업로드)

```
파일 선택
  → POST /voc/preview  (컬럼 목록 + 샘플 3행 반환)
  → 사용자가 컬럼 매핑 설정
  → POST /voc/upload   (매핑 JSON 포함)
      → 500행 청크 단위 파싱
      → 각 행: analyze() → VocRecord + VocAnalysis 저장
      → 청크 단위 DB 커밋
  → 결과 반환 (성공 건수, 실패 행 상세)
```

---

## 7. 향후 로드맵

| 단계 | 내용 |
|---|---|
| **v1.1** | LLM 분석 엔진 교체 (OpenAI / Claude API) |
| **v1.2** | PDF 리포트 다운로드 |
| **v1.3** | 멀티테넌트 조직(Organization) 단위 데이터 분리 |
| **v2.0** | 실시간 API 연동 (앱스토어·리뷰 플랫폼 자동 수집) |
| **v2.1** | Slack / 이메일 알림 (부정 급증 감지 시) |
