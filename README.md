# axhub-app

풀스택 웹 애플리케이션 — Next.js (frontend) + FastAPI (backend)

## 개발 실행

**Frontend** (터미널 1)
```bash
cd frontend
npm run dev
# http://localhost:3000
```

**Backend** (터미널 2)
```bash
cd backend
uv run uvicorn main:app --reload
# http://localhost:8000
```
